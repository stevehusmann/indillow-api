{
  "success_url": "/",
  "env": {
    "APP_ENV": "prod",
    "APP_SECRET": { "generator": "secret" },
    "CORS_ALLOW_ORIGIN": "https://indillow.vercel.app"
  },
  "addons": ["heroku-postgresql"],
  "buildpacks": [
    {
      "url": "https://github.com/jontewks/puppeteer-heroku-buildpack"
    }
  ],
  "scripts": {
    "postdeploy": "php bin/console doctrine:schema:create"
  }
}
